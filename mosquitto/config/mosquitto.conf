# ── MQTT Listener (for ESP32 and Python) ──
listener 1883
allow_anonymous true

# ── WebSocket Listener (for browser clients) ──
listener 9001
protocol websockets
allow_anonymous true

# ── Logging ──
log_type all
log_dest stdout